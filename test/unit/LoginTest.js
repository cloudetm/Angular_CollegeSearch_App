/**
 * Created by vaibhavid on 03-12-2014.
 */
'use strict';
/*
describe('Login Controllers', function() {
    beforeEach(module('CollegeApp'));
    var scope;
    var ctrl;


    beforeEach(inject(function($rootScope, $controller,$location) {
        scope = $rootScope.$new();

        ctrl = $controller('LoginController', {$scope: scope , $location:$location});
        this.locationPath = spyOn($location,'path');

    }));

    //If we provide correct username and password it will go to welcome page else not.
    describe('$scope.login', function() {
        it('should succesfully redirect to /welcome', function() {
            scope.username = "Vaibhavi",
                scope.password = "Vaibhavi@1"
            scope.login();
            expect(this.locationPath).toHaveBeenCalledWith('/welcome');
        });

        it('should not call login function', function() {
            scope.username = "V",
                scope.password = "@1"
            scope.login();
            expect(this.locationPath).not.toHaveBeenCalledWith('/welcome');

        });
    });
});
*/

